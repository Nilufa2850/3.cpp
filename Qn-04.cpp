//WAP in c++ to find the max of two numbers.
#include<iostream>
using namespace std ;
int main ()
{
    int x , y ;
    cout<<"Enter two numbers : " ;
    cin>>x>>y ;
    if (x>y)
        cout<<"x="<<x<<" is max";
    else
        cout<<"y="<<y<<" is max" ;
    return 0 ;
}
